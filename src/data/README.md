# /src/data

Reserved for dummy JSON data (books, chapters, users, etc.) used in place of a
real backend, per the project rule "No backend. Use dummy JSON data."

This folder is intentionally empty in the foundation module — actual dummy
datasets belong to the feature modules that consume them (e.g. Book Library,
Auth) and will be added when those modules are built.
